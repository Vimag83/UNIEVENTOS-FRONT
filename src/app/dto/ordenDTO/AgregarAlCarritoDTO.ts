import { LocalidadEventoDTO } from "../eventoDTO/LocalidadEventoDTO"

export interface AgregarAlCarritoDTO{
        idCuenta:object,
        idEvento: object,
        localidad: string,
        cantidad: number

}
